plugins {
    id 'io.nextflow.nextflow-plugin' version '1.0.0-beta.10'
}

version = '0.1.0'

dependencies {
    implementation 'io.jhdf:jhdf:0.10.0'
}

nextflowPlugin {
    nextflowVersion = '24.10.0'

    provider = 'Nico Trummer'
    className = 'acme.plugin.AnnDataPlugin'
    extensionPoints = [
        'acme.plugin.AnnDataExtension'
    ]

}
